<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter&family=Noto+Sans:ital,wght@0,100..900;1,100..900&family=Nunito:ital,wght@0,200..1000;1,200..1000&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
        rel="stylesheet">
        <link rel="stylesheet" href="/index.css">
</head>

<body>
    <header>
        <h3 class="logo">Xem phim</h3>
         <button class="top-phim">topphim</button>
        <form action="/search" method="post"> <input class="entersearch" type="text" name="search" id=""> <input class="searchok" type="submit" value="Submit">
        </form>
    </header>
    <section class="content">
        <% if(locals.play) {%>
        <iframe class="film" src="<%= play %>"
            title="Iframe Example" allowfullscreen></iframe> <% } %>
            <% if(locals.title) {%>
            <div class="title"><h3><%=title %></h3></div> 
            <% } %>
            <div class="ep">
                <% if (locals.ep) { %>  
                    <% for (let i = 0; i < locals.ep; i++) { %> 
                        <input class="btn" name="epp" type="submit" value="<%=i+1%>">
                    <% } %> 
                <% } %>
                
            </div>
    </section>
    <section class="recomand">dà</section>
</body>

</html>